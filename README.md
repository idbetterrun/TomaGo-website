# TomaGo Page

当前推荐使用 `index.html` 作为页面入口。

目录说明：
- `index.html`：拆分后的正式入口文件
- `assets/css/styles.css`：页面样式
- `assets/js/tailwind-config.js`：Tailwind 配置
- `assets/js/app.js`：页面交互逻辑
- `TomaGo.html`：原始单文件版本备份

如果只是本地预览，直接用浏览器打开 `index.html` 即可。
